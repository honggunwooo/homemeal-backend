spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fooddb
    username: root
    password: Rlcjf8645//
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
